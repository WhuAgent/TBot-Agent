现在的任务是：{task}

页面中包含的元素如下：

{html_description}

页面中表单数据的状态如下：

{form_data_status}

请逐步思考：为了完成任务需要哪些信息？要怎么找到对应的元素并与其进行交互。完成思考之后直接返回一个 agent 执行的动作列表，作为你的计划，形式如下：

[
    {
        "action": "以函数调用的方式表示的要执行的动作",
        "args": "如果该动作的执行需要参数，请以字典形式给出每一个参数的值",
        "think": "进行该动作的理由"
    }
]