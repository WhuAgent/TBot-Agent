现在的任务是：{task}

请逐步思考：为了完成任务需要进行哪些动作。完成思考之后直接返回一个 agent 执行的动作列表，作为你的计划，形式如下：

[
    {
        "action": "以函数调用的方式表示的要执行的动作",
        "args": "如果该动作的执行需要参数，请以字典形式给出每一个参数的值",
        "think": "进行该动作的理由"
    }
]