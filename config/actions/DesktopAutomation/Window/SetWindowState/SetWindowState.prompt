def SetWindowState(self, window_name, status, way):
    """
    更改窗口显示状态
    ---
    参数：
        window_name: 窗口名称，格式为“名称 - 程序名”，例：“1.txt - 记事本”
        status: 窗口的状态，共五种值：Hide（隐藏窗口）,Show（显示窗口）,Maximize（最大化窗口），Minimize（最小化窗口），Restore（还原窗口）
        way:  查找窗口的方式，有四种方式，分别为：包含， 头部匹配， 尾部匹配， 精确匹配，值分别为：Contains, Start with, End with, Exact match，默认为包含，即“Contains”。，同CheckWindowNameExists
    返回值：
        无返回值
    """