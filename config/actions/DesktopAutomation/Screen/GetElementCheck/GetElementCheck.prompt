def GetElementCheck(element_name, handle_obj):
    """
    根据元素句柄判断元素是否被勾选。
    ---
    参数：
        element_name: 元素主题
        handle_obj: 句柄类型变量，用来指定元素
    返回值：
        bool_obj: 布尔类型值，True表示被勾选，False表示没被勾选
    """