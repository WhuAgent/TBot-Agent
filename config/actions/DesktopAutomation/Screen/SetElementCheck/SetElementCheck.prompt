def SetElementCheck(element_name, handle_obj, status):
    """
    根据元素句柄设置元素的勾选状态，即勾选或不勾选。
    ---
    参数：
        element_name: 元素主题
        status: 设置勾选状态。是表示勾选，否表示不勾选。
        handle_obj: 句柄类型变量，用来指定元素
    返回值：
        无返回值
    """