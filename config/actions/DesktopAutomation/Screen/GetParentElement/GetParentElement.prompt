def GetParentElement(element_name, handle_obj, layer):
    """
    根据元素句柄获取指定父层级的父元素句柄。
    ---
    参数：
        element_name: 元素主题
        layer: 父元素级别，只能是正整数， 如1，2，3
        handle_obj: 句柄类型变量，用来指定元素
    返回值：
        parent_handle: 父元素句柄
    """