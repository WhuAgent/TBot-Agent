def GetElementRect(element_name, handle_obj):
    """
    根据元素句柄获取元素的区域。
    ---
    参数：
        element_name: 元素主题
        handle_obj: 句柄类型变量，用来指定元素
    返回值：
        rect: 字典类型值，包含描述区域的x, y, height, width
    """