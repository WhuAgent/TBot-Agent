def ExcelDeleteWorksheet(self, sheet_name):
    """
    删除指定的工作表。
    ---
    参数:
        v_Excel: 需要删除工作表所在excel的对象，默认为v_Excel
        sheet_name: 需要删除的工作表名称，如果使用字符串，则表示指定工作表的名字；使用数字，则表示指定工作表的顺序（从0开始）
    返回值:
        无返回值
    """