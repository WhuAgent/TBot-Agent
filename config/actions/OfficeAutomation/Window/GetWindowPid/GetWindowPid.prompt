def GetWindowPid(self,window_name,way):
   """
   获取窗口进程
   ---
   参数：
       window_name: 窗口名称，格式为“名称 - 程序名”，例：“1.txt - 记事本”
       way:  查找窗口的方式，有四种方式，分别为：包含， 头部匹配， 尾部匹配， 精确匹配，值分别为：Contains, Start with, End with, Exact match，默认为包含，即“Contains”。
   返回值：
       window_pid: 窗口进程
   """