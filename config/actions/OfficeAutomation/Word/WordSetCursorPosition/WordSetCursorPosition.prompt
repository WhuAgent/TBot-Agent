def WordSetCursorPosition(document, move_num, move_type):
    """
    设置光标在文档中移动的位置
    ---
    参数：
        document: 要操作的文档对象
        move_num: 光标移动次数
        move_type: 光标移动方式，可选值有 "行", "段落", "字符" 三个值，默认值为 "行"
    返回值：
        无返回值
    """