def PTSDrawText(position, area, content, char_size, color):
    """
    在屏幕上绘制文字
    ---
    参数：
        position: 屏幕的分辨率以及绘制文字左上角坐标，格式为[横向分辨率,纵向分辨率,横向坐标,纵向坐标]
        area: 允许绘制的范围，一般默认全屏，为[横向分辨率,纵向分辨率]
        content: 内容列表，不同列表元素为换行，例如：["窗前明月光，","疑是地上霜。"]
        char_size: 字体大小，默认为24
        color: 字体颜色，范围0~255，格式为[红,绿,蓝]
    返回值：
        pr_result: 布尔类型，返回执行结果，执行成功后返回值是true
    """