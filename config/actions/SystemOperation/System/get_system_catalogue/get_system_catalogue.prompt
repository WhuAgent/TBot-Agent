def get_system_catalogue(target):
    """
    获取系统文件夹路径。
    ---
    参数：
        target: 要获取哪一个文件夹，为 “系统目录” 则获取系统文件夹路径；为 “windows目录” 则获取Windows文件夹路径；为 “桌面目录” 则获取桌面路径；为 “软件安装目录” 则获取软件安装目录路径；为 “开始菜单目录” 则获取开始菜单文件夹路径
    返回值：
        系统文件夹路径
    """